# SQL Scripts

Store reusable SQL scripts for Medi_Track here.

## Structure

- `oracle/` - Oracle-specific scripts
- `mysql/` - MySQL-specific scripts (if needed later)

## Example

- `oracle/setup-user.sql` - creates the Oracle user and grants permissions
- `oracle/sample-queries.sql` - starter queries for development and testing
